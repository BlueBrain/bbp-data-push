
###### Base command script:

python bba_dataset_pusher.py \
  --forge_config_file "../tests/configuration/forge_config.yml" \
  --env XXX \
  --org XXX \
  --proj XXX \
  --token XXX \


###### Concatenate the Base command with one of these:

### Push a hybrid annotation file and a density folder:

  push-volumetric \
  --dataset_path '/cell_density' \
  --dataset_path '/annotation_hybrid.nrrd' \
  --config '/push_dataset_config_template.yaml' \
  --voxels_resolution '25'

### Push a batch of 3 meshes with 2 extra informations:

 push-meshes \
  push-meshes \
  --dataset_path '/brain_region_meshes' \
  --config '/push_dataset_config.yaml'
