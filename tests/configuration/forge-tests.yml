Model:
  name: ShaclModel
  origin: directory
  source: None

Store:
  name: BlueBrainNexus
  endpoint: https://staging.nexus.ocp.bbp.epfl.ch/v1
  bucket: dke/tests
  token: <token>
  versioned_id_template: "{x.id}?rev={x._store_metadata._rev}"
  file_resource_mapping: ../configurations/file-to-resource-mapping.hjson

Resolvers:

Formatters:
  identifier: https://kg.example.ch/{}/{}
