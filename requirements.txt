# =============================================================================
# DEPRECATION WARNING:
#
# The file `requirements.txt` does not influence the package dependencies and
# will not be automatically created in the next version of PyScaffold (v4.x).
#
# Please have look at the docs for better alternatives
# (`Dependency Management` section).
# =============================================================================
#
# Add your pinned requirements so that they can be easily installed with:
# pip install -r requirements.txt
# Remember to also add them in setup.cfg but unpinned.
# Example:
# numpy==1.13.3
# scipy==1.0

pytest>=7.0.1
pytest-cov>=2.8.0
nexusforge==0.8.1
# copying from setup.install_requires
PyJWT>=2.0.0
click>=7.0
h5py>=2.10.0
pynrrd>=0.4.0
rdflib==6.2.0
urllib3
blue_brain_atlas_web_exporter @ git+https://gitlab-ci-token:${CI_JOB_TOKEN}@bbpgitlab.epfl.ch/dke/apps/blue_brain_atlas_web_exporter.git@v2.0.2
